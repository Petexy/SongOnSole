# What America writes differently, and nothing else.
#
# This is an overlay, not a second catalog: every message here must be one
# en-GB.ftl already has, must take the same variables, and must actually say
# something else. `Catalog::validate` holds it to all three.

favourites = Favorites
favourite = Add to favorites
unfavourite = Take out of favorites
